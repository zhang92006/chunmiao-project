# 扰动范围说明

本文整理当前 `scenario_reconstruction` 中“扰动扩增”功能到底支持哪些扰动范围、如何生效、哪些写法现在还不支持。

## 1. 扰动的入口在哪里

当前扰动定义写在事故模板的 `perturbations` 字段里，例如 [autoware_cut_in.yaml](g:/chunmiao/d2rl/Dense-Deep-Reinforcement-Learning/scenario_reconstruction/templates/autoware_cut_in.yaml)：

```yaml
perturbations:
  - field: ego.speed
    distribution: uniform
    low: -2.0
    high: 2.0
  - field: actors.BV_cut_in.position
    distribution: uniform
    low: -5.0
    high: 5.0
  - field: actors.BV_cut_in.speed
    distribution: uniform
    low: -2.0
    high: 2.0
  - field: events.forced_bv_action.start_time
    distribution: uniform
    low: 0.0
    high: 1.0
```

批量生成时，`augment_templates.py` 会对每个扰动项采样一个随机增量 `delta`，然后加到原始模板值上。

也就是说，当前机制是：

```text
新值 = 原始值 + 随机扰动增量
```

不是“在 low 和 high 之间直接重新采样最终值”，而是“对基准模板做偏移”。

## 2. 当前支持的扰动路径

当前代码只支持三类 `field` 路径：

```text
ego.<字段>
actors.<actor_id>.<字段>
events.<event_type>.<字段>
```

对应实现位置在 [augment_templates.py](g:/chunmiao/d2rl/Dense-Deep-Reinforcement-Learning/scenario_reconstruction/augment_templates.py) 的 `_apply_delta()`。

### ego.<字段>

用于扰动自车 `ego` 的数值字段。

当前模板结构下，理论上可扰动的典型字段包括：

```text
ego.position
ego.speed
ego.lane_index
```

但要注意，虽然代码层面会做数值加法，`lane_index` 这类离散字段并不适合直接用连续扰动。当前真正合理使用的主要是：

```text
ego.position
ego.speed
```

### actors.<actor_id>.<字段>

用于扰动某一辆背景车的字段。

例如：

```text
actors.BV_cut_in.position
actors.BV_cut_in.speed
actors.BV_right_follower.position
actors.BV_right_follower.speed
```

同样地，代码允许数值相加，但从场景语义上最适合扰动的是：

```text
position
speed
```

如果去扰动 `lane_index`，虽然语法上可能成立，但当前 `2Lane` 场景下容易造成不合理初始化，通常不建议直接这么做。

### events.<event_type>.<字段>

用于扰动事件本身的参数。

当前代码是通过 `event.type` 找到事件，因此：

```text
events.forced_bv_action.start_time
events.forced_bv_action.duration
events.perception_dropout.start_time
events.perception_dropout.duration
```

这类路径在语法上都支持。

但需要特别注意两点：

```text
它是按 event.type 查找，不是按事件序号查找
如果同一种 type 出现多次，当前只会命中第一条
```

所以当前最稳妥的写法是：每种 `type` 在一个模板里只放一条，或者后续我们把事件寻址机制升级成按名字或按索引定位。

## 3. 当前支持的扰动分布

目前只支持一种分布：

```text
uniform
```

也就是均匀分布。

验证逻辑在 [templates.py](g:/chunmiao/d2rl/Dense-Deep-Reinforcement-Learning/scenario_reconstruction/templates.py)：

```python
if item.distribution != "uniform":
    raise ValueError("Only uniform perturbations are supported for now.")
```

因此现在还不支持：

```text
normal
gaussian
choice
categorical
loguniform
```

如果模板里写这些名字，当前会直接校验失败。

## 4. 当前 low / high 的含义

当前 `low` 和 `high` 表示“扰动增量范围”，不是最终值范围。

例如模板里：

```yaml
- field: ego.speed
  distribution: uniform
  low: -2.0
  high: 2.0
```

若基准速度是：

```text
ego.speed = 34.8
```

那么生成时实际会变成：

```text
34.8 + delta
delta ~ Uniform(-2.0, 2.0)
```

所以最终速度范围是：

```text
[32.8, 36.8]
```

不是 `[ -2.0, 2.0 ]`。

## 5. 当前模板里实际使用的扰动范围

当前 `autoware_cut_in.yaml` 实际定义了 4 个扰动项：

### ego.speed

```text
基准值：34.8
扰动增量范围：[-2.0, 2.0]
最终范围：[32.8, 36.8]
```

### actors.BV_cut_in.position

```text
基准值：418.0
扰动增量范围：[-5.0, 5.0]
最终范围：[413.0, 423.0]
```

### actors.BV_cut_in.speed

```text
基准值：24.5
扰动增量范围：[-2.0, 2.0]
最终范围：[22.5, 26.5]
```

### events.forced_bv_action.start_time

```text
基准值：0.0
扰动增量范围：[0.0, 1.0]
最终范围：[0.0, 1.0]
```

这 4 个量共同决定了：

```text
切入何时发生
切入时两车相对间距是多少
切入时两车相对速度是多少
碰撞是否发生
碰撞在什么时候发生
```

## 6. 当前不建议或不支持的扰动

下面这些情况，要么当前不支持，要么虽然代码能跑但不建议现在直接用。

### 不支持非数值字段

当前 `_apply_delta()` 本质是做：

```python
target[key] = float(target[key]) + delta
```

因此下面这类字段当前不能直接扰动：

```text
controller
route
id
role
params.lateral
params.target
```

因为它们不是数值，无法做 `float(...) + delta`。

### 不支持事件 params 的子字段路径

当前仅支持：

```text
events.<type>.<字段>
```

不支持更深层路径，例如：

```text
events.forced_bv_action.params.longitudinal
events.perception_position_bias.params.offset_x
```

如果后面要扰动事件参数内部字段，需要扩展 `_apply_delta()` 的路径解析逻辑。

### 不建议直接扰动车道编号

虽然 `lane_index` 是数值，但当前是离散语义字段，不适合直接加连续噪声。

例如：

```text
1 + 0.37 = 1.37
```

这种值在模板校验时就会失去意义。即使最后转换成整数，也会带来额外歧义。

如果后面要支持“换到另一条车道”这种扰动，更合理的方法是：

```text
单独定义离散扰动
或者定义枚举选择型扰动
```

## 7. 当前扰动范围的约束

除了上面的语法限制，当前还有这些约束：

```text
low 必须小于等于 high
速度最终不能小于 0
位置最终不能小于 0
事件结束时间不能超过场景 duration
当前地图只支持 2Lane
lane_index 当前只允许 0 或 1
```

如果扰动后违反这些条件，`ScenarioTemplate.from_dict()` 校验会失败，该变体不会被接受。

## 8. 当前最适合继续扩展的扰动项

如果后面要继续做更丰富但仍然稳定的样本扩增，建议优先加这些数值扰动：

```text
ego.position
actors.BV_right_follower.position
actors.BV_right_follower.speed
events.forced_bv_action.duration
```

如果再往前走一步，可以扩展支持：

```text
events.forced_bv_action.params.longitudinal
perception_position_bias 的偏移量参数
control_delay 的延迟步数参数
perception_delay 的延迟时间参数
```

不过这些需要先把事件参数级扰动和对应事件执行逻辑补齐。

## 9. 一句话总结

当前扰动范围的本质是：

```text
对模板中的数值型初始条件和事件时间做 uniform 增量扰动
```

目前最成熟、最稳定、真正已经跑通的扰动对象是：

```text
自车速度
背景车位置
背景车速度
强制 cut-in 触发时间
```
