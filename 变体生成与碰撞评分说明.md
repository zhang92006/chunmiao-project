# 变体生成与碰撞评分说明

本文说明当前 `scenario_reconstruction` 中“扰动扩增批量生成”的实现方式，以及为什么现在不再是全程强行控制车辆，而是只给初始约束和短时故障触发，让仿真自然演化出不同中间过程。

## 1. 当前如何创造变体

当前变体生成入口是：

```bash
python -m scenario_reconstruction.run_batch scenario_reconstruction/templates/autoware_cut_in.yaml --count 5 --seed 7 --output_root data_analysis/raw_data/ScenarioReconstructionSnapshotScored
```

内部顺序是：

```text
读取事故模板
读取模板中的 perturbations 扰动范围
按 seed 对每个扰动项做均匀采样
把采样值加到模板原始值上
写出多个 variant JSON
逐个送入 SUMO/NADE 环境运行
统计碰撞结果、训练字段和接近度评分
```

也就是说，当前不是让智能体输出 `epsilon` 来生成变体。`epsilon` 属于 D2RL 训练阶段智能体要学习和输出的动作，不应该在事故重建阶段被固定成“控制脚本”。事故重建阶段只做一件事：给环境提供一批合理不同的初始条件和故障触发条件，让 SUMO/NADE 自己演化。

## 2. 当前扰动了哪些量

当前模板 `autoware_cut_in.yaml` 中扰动的是：

```text
ego.speed
actors.BV_cut_in.position
actors.BV_cut_in.speed
events.forced_bv_action.start_time
```

含义分别是：

```text
自车初始速度
切入车初始纵向位置
切入车初始速度
切入故障动作触发时间
```

这些量会影响碰撞是否发生、碰撞发生时间、碰撞前 TTC、相对速度、横向位置差等中间过程。

## 3. 为什么说不是全程控制

模板里仍然有一个 `forced_bv_action`，但它现在是短时故障触发，不是整段轨迹重放。

当前配置类似：

```yaml
event_type: forced_bv_action
actor: BV_cut_in
start_time: 0.0
duration: 0.8
params:
  lateral: left
  longitudinal: 0.0
  apply_once: false
```

含义是：在一个很短的时间窗口里，给 `BV_cut_in` 一个切入动作，让故障场景被触发；之后车辆继续由 SUMO/NADE 环境中的车辆动力学、车道位置和周围车交互自然推进。

所以当前生成不同中间过程的来源是：

```text
初始位置不同
初始速度不同
故障触发时刻不同
短时切入后自然演化不同
```

这比“全程按固定轨迹控制车辆”更适合作为后续训练数据扩增，因为每条样本的碰撞过程、风险演化和 reward 都可能不同。

## 4. 训练 reward 如何变化

D2RL 训练环境中的 reward 仍然由训练环境根据 `epsilon` 和 `ndd_possi` 计算。当前重建阶段只负责给每个候选样本写入可训练字段，例如：

```text
drl_obs_step_info
ndd_step_info
criticality_step_info
weight_step_info
```

其中 `ndd_possi` 和 `criticality` 现在不再是模板里固定写死的常数，而是根据仿真时刻的几何风险估计出来，例如距离、相对速度、TTC 等。因此不同变体会得到不同的 `ndd_possi`，在同一个测试 epsilon 下也会得到不同 reward。

## 5. 为什么只看碰撞时间还不够

只看 `end_time` 会有一个问题：两个样本都可能在 1.3s 或 1.5s 左右碰撞，但碰撞瞬间车辆状态可能完全不同。

例如需要额外比较：

```text
碰撞对象是否一致
碰撞瞬间自车位置和速度
碰撞瞬间对方车位置和速度
两车纵向距离
两车横向距离
两车相对速度
碰撞前一小段时间的 TTC
碰撞前一小段时间的最近距离
车道是否一致
```

因此现在 `run_batch.py` 会从每条 episode 的 `av_obs` 中提取碰撞快照。

## 6. 碰撞快照记录了什么

`batch_summary.json` 中每条 outcome 现在包含：

```text
collision_snapshot
collision_snapshot_penalty
closeness_score
```

其中 `collision_snapshot` 包含：

```text
timestep
ego
actor
relative_longitudinal_distance
relative_lateral_distance
relative_speed
previous_ttc
previous_distance
```

`ego` 和 `actor` 内部记录：

```text
veh_id
position
velocity
lane_index
acceleration
heading
```

这样就可以检查“是不是同一种事故”，而不是只检查“是不是差不多同一秒撞了”。

## 7. 当前接近度评分

当前 `closeness_score` 越低，表示越接近目标事故样本。

评分项包括：

```text
终止时间与目标事故时间的差距
最小 TTC
最小距离
是否发生碰撞
是否与目标车辆 BV_cut_in 碰撞
碰撞瞬间车辆状态惩罚 collision_snapshot_penalty
```

其中 `collision_snapshot_penalty` 进一步考虑：

```text
碰撞瞬间纵向距离
碰撞瞬间横向距离
碰撞瞬间相对速度
碰撞瞬间两车车道差
```

后续如果有真实 Autoware 事故样本的精确终止状态，也可以把当前“目标事故时间 + 目标碰撞对象”的评分升级为“与真实事故终止状态逐项对齐”的评分。

## 8. 当前验证结果

本次运行命令：

```bash
conda run -n D2RL python -m scenario_reconstruction.run_batch scenario_reconstruction/templates/autoware_cut_in.yaml --count 5 --seed 7 --output_root data_analysis/raw_data/ScenarioReconstructionSnapshotScored --max_ndd_possi 0.01 --target_end_time 1.4 --target_collision_actor BV_cut_in
```

运行结果：

```text
successful_runs=5
failed_runs=0
training_ready_crashes=5
```

5 组都发生了碰撞，碰撞对象都是：

```text
['CAV', 'BV_cut_in']
```

按 `closeness_score` 从低到高排序：

```text
2.json end=1.3 score=0.7621 snapshot_penalty=0.6319
3.json end=1.5 score=0.8380 snapshot_penalty=0.7029
0.json end=1.1 score=0.9452 snapshot_penalty=0.6111
4.json end=1.9 score=1.1093 snapshot_penalty=0.5748
1.json end=1.1 score=2.3167 snapshot_penalty=1.0167
```

可以看到，虽然 0.json 和 1.json 都是在 1.1s 左右碰撞，但 1.json 的横向距离和相对速度更偏离，因此总分更差。这说明现在已经不只是按碰撞时间排序，而是开始考虑碰撞瞬间和碰撞前状态。

## 9. VALID_FAILURE_TYPES 各事件含义

`templates.py` 中当前允许的故障事件类型是：

```python
VALID_FAILURE_TYPES = {
    "perception_dropout",
    "perception_delay",
    "perception_position_bias",
    "control_delay",
    "forced_bv_action",
}
```

它们的含义可以理解为：

### perception_dropout

表示“感知丢失”。

典型含义是：在某个时间窗口内，Autoware 或上层决策模块看不到某个目标车，或者目标车目标框/目标状态直接消失。

如果以后在仿真里真正实现，这类故障通常会表现为：

```text
某辆车在感知结果里暂时被删除
或者某类障碍物整体不再上报
```

它主要影响的是“看没看到目标”。

### perception_delay

表示“感知延迟”。

典型含义是：系统拿到的不是当前时刻目标状态，而是若干毫秒之前的旧状态。

如果以后在仿真里真正实现，这类故障通常会表现为：

```text
目标位置、速度、航向使用历史帧
规划控制基于滞后的环境信息做决策
```

它主要影响的是“看到的是不是过时信息”。

### perception_position_bias

表示“感知位置偏差”。

典型含义是：感知到了目标，但目标的位置、速度或朝向有系统性偏移，例如横向偏 0.5 米、纵向偏 2 米。

如果以后在仿真里真正实现，这类故障通常会表现为：

```text
目标仍然存在
但其上报坐标与真实坐标不一致
```

它主要影响的是“看到了，但看偏了”。

### control_delay

表示“控制延迟”。

典型含义是：规划层已经给出了转向、制动、加速度指令，但执行到底盘时有延迟。

如果以后在仿真里真正实现，这类故障通常会表现为：

```text
当前控制命令不会立刻执行
而是在若干步之后才作用到车辆
```

它主要影响的是“想做的动作没有及时做出来”。

### forced_bv_action

表示“强制背景车动作注入”。

这是当前已经在 `scenario_reconstruction/environment.py` 里真正接入执行的事件。它的作用不是修改自车感知，也不是修改控制链路，而是直接给某一辆背景车一个指定动作，例如：

```text
强制变道
强制切入
附加纵向动作
```

当前我们用它来触发 `BV_cut_in` 的 cut-in 事故，是现在这套事故重建里最核心、最可用的事件。

## 10. 当前实现状态

这 5 类事件目前要分成“已实现执行”和“仅完成模板注册”两类来看：

```text
forced_bv_action：已接入仿真执行
perception_dropout：已允许在模板中声明，但尚未真正注入感知链
perception_delay：已允许在模板中声明，但尚未真正注入感知链
perception_position_bias：已允许在模板中声明，但尚未真正注入感知链
control_delay：已允许在模板中声明，但尚未真正注入控制链
```

所以现在如果模板里写了 `perception_dropout` 这类事件，含义上是成立的，但默认还不会像 `forced_bv_action` 那样直接改变当前 SUMO 运行结果，除非我们下一步把它们真正接到感知/控制数据流里。
